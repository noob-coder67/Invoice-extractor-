# Invoice Extractor

This project is a FastAPI service that extracts structured data from invoices and contracts.

## How to use
- Deploy with Docker (Render free tier works).
- Send invoice text to `/extract/text` endpoint.
- Receive structured JSON with confidence scores and validation issues.
